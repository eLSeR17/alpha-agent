# AlphaAgent – core dependencies
pydantic>=2.0,<3.0
httpx>=0.24,<1.0

# Financial data (free, no API key)
yfinance>=0.2.40,<1.0
numpy>=1.23
pandas>=1.5,<3.0

# HTTP API server
fastapi>=0.110,<1.0
uvicorn>=0.29,<1.0

# Optional: OpenAI-compatible backend (gpt-4o-mini, Groq, vLLM, ...)
# Install with: pip install openai
openai>=1.30,<2.0

# Testing
pytest>=7.0,<10.0
